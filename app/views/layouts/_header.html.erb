<header class="navbar navbar-fixed-top">
  <div class="navbar-inner">
    <div class="container">
      <%= link_to "craven", '#', id: "logo" %>
      <nav>
        <ul class="nav pull-right">
          <li><% unless params[:controller] == 'static_pages' && params[:action] == 'home' %> <%= render 'layouts/search_bar' %> <% end %></li>
          <li><%= link_to "Home",    root_path %></li>
          <li><%= link_to "Restaurants", restaurants_path %></li>
          <% if user_signed_in? %>
            <%= render 'layouts/signed_in_menu' %>
          <% else %>
            <li><%= link_to "Sign in", new_user_session_path %></li>
          <% end %>
        </ul>
      </nav>
    </div>
  </div>
</header>